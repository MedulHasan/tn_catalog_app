name: Android build

runs:
  using: 'composite'
  defaults:
    run:
      shell: bash
  steps:
    - name: Print
      run: echo "Android building"

    - name: Download Android Keystore file
      run: echo "Test Download"
